# Test report

Canonical run report: **[../../test-report.md](../../test-report.md)** (repo root). Inventory is ordered **by importance** (assignment uniqueness proofs first, then delivery, access, workers, HTTP hygiene, unit config).

Update that file after a full `./mvnw test`. Strategy plans stay in this folder ([README.md](README.md)).
