spring:
  config:
    activate:
      on-profile: dev
  flyway:
    locations: classpath:db/migration,classpath:db/demo

app:
  appointments:
    one-confirmed-per-vehicle: ${APP_ONE_CONFIRMED_PER_VEHICLE:false}
